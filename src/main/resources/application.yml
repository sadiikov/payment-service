spring:
  application:
    name: payment-project
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_project
    username: postgres
    password: root123
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080